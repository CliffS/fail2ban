{
  "name": "fail2ban",
  "version": "1.0.2",
  "description": "Javascript interface to the fail2ban server",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "coffee --watch --compile --output lib src &",
    "build": "coffee --compile --output lib src",
    "prepublishOnly": "npm run-script build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/drudrum/fail2ban.git"
  },
  "keywords": [
    "fail2ban-client",
    "fail2ban-server"
  ],
  "author": "Cliff Stanford <cliff@may.be>",
  "license": "Unlicense",
  "bugs": {
    "url": "https://github.com/CliffS/fail2ban/issues"
  },
  "homepage": "https://github.com/CliffS/fail2ban#readme",
  "devDependencies": {
    "coffeescript": "^2.3.2"
  },
  "dependencies": {    
  }
}
